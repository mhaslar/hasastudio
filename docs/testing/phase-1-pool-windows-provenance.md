# Windows FramePool evidence

The owner supplied [this JSON](phase-1-pool-windows-x86_64.json) directly in
the conversation on 2026-09-06 after running `rezie-pool-check` on the
reference machine. Values are preserved as supplied. The report does not
embed a checkout revision or toolchain; neither is inferred here.

The functional ownership check passes. No shader, golden, performance or
five-minute steady-state allocation criterion was evaluated by this command.
